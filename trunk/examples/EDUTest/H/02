maxChildDur = 20;
tempo = "quarter=60";
timeSignature = "4/4";
EDUPerBeat = 3;

numChildren = <"FIXED">;

childNames = < <"B/sound00"> >;

childEventDef = < "CONTINUUM", 
		Select(<0.3>, CURRENT_CHILD_NUM), "PERCENTAGE",
		Select(<0>, CURRENT_CHILD_NUM),
		Select(<0.9>, CURRENT_TYPE), "PERCENTAGE" >;
		

